node_modules/
dist/
coverage/
*.tgz
*.log
.DS_Store

# Credenciais locais (ex.: ANTHROPIC_API_KEY para npm run discover:ai) — nunca commitar.
.env
.env.*

# Gerado a cada rodada do pipeline; vira corpo do PR automático, não precisa ser versionado.
pipeline/report.md

# Saídas das ferramentas de descoberta (npm run discover / discover:ai) — material de curadoria.
pipeline/discovery-report.md
pipeline/discovery-sheet.png
pipeline/discovery-ai-report.md
pipeline/discovery-ai-sheet.png
